{% extends 'base.html.twig' %}

{% block title %}PetBuddy{% endblock %}

{% block body %}

    <div class="row">
        {% for message in app.flashes('users_add_success') %}
            <div class="alert alert-success" role="alert">
            Felicitation!! {{ message }}
    </div>
        {% endfor %}
    
    <h1> Ajouter un Membre </h1> 

    {{ form_start(formContactMessage)}}
        <div class="row">
            {{form_label(formContactMessage.users, 'Id utilisateurs')}}
            {{form_widget(formContactMessage.users )}}
        </div>
        <div class="row">
            {{form_label(formContactMessage.announces, 'Id annonces')}}
            {{form_widget(formContactMessage.announces)}}
        </div>
        <div class="row">
            {{form_label(formContactMessage.message, 'Message')}}
            {{form_widget(formContactMessage.message)}}
        </div>
        <div class="row">
            {{form_label(formContactMessage.animalType, 'Type d\'animal')}}
            {{form_widget(formContactMessage.animalType)}}
        </div>
        <div class="row">
            {{form_label(formContactMessage.agreeTerms)}}
            {{form_widget(formContactMessage.agreeTerms)}}
        <div class="row">
            {{form_label(formContactMessage.rgpd)}}
            {{form_widget(formContactMessage.rgpd)}}
        </div>
        <br>
        <button type="submit" class="btn btn-success btn-sm mb-1">Enregistrer</button>
    {{ form_end(formUsers)}}
        <br>

{% endblock %}